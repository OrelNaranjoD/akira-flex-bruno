meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{URL}}auth/platform/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@akiraflex.com",
    "password": "SuperAdmin123!"
  }
}

vars:post-response {
  ACCESS_TOKEN: res.body.accessToken
}
