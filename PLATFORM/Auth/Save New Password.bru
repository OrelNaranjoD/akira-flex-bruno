meta {
  name: Save New Password
  type: http
  seq: 6
}

patch {
  url: {{URL}}auth/reset-password?token={{TOKEN}}
  body: json
  auth: none
}

params:query {
  token: {{TOKEN}}
}

body:json {
  {
    "password": "SuperAdmin456!"
  }
}

vars:post-response {
  ACCESS_TOKEN: res.body.accessToken
}
